# Commands to open and run the Docusaurus book\n\n# 1. Navigate to the book directory\ncd d:\\piaic_hackathon\\my-book\n\n# 2. Install dependencies (if not done before)\nnpm install\n\n# 3. Start the development server to view the book locally\nnpm run start\n\n#   This will launch the book at http://localhost:3000 (or the port shown in the console).\n#   Press Ctrl+C to stop the server.\n\n# 4. Build the static site (optional, for deployment)\nnpm run build\n\n#   The built files will be placed in the `build` folder.\n\n# 5. Serve the built site locally (optional)\nnpm run serve\n\n#   This will serve the static site on a local port for preview.\n\n# Note: Ensure you have Node.js (>=14) and npm installed.\n# You can check with `node -v` and `npm -v`.
